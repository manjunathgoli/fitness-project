
let sidemenu = document.querySelector('.side-menu');
let btnMenuHome = document.querySelector(".btnMenuHome");
let isShowmenu = true;
function showmenu(){
  if(isShowmenu){
    sidemenu.style.display = "block";
    isShowmenu = false;
  }
  else{
    sidemenu.style.display = "none";
      isShowmenu = true;
  }
}

let popup_shoulder = document.querySelector(".popup-shoulder");
let isShow_shoulder = true;
function showshoulder(){
  if(isShow_shoulder){
  popup_shoulder.style.display = "block";
  isShow_shoulder = false;
  }
  else{
      popup_shoulder.style.display = "none";
  isShow_shoulder = true;
  }
}
let popup_bicep = document.querySelector(".popup-bicep");
let isShow_biceps = true;
function showbicep(){
  if(isShow_biceps){
  popup_bicep.style.display = "block";
  isShow_biceps = false;
  }
  else{
      popup_bicep.style.display = "none";
  isShow_biceps = true;
  }
}
let popup_Triceps = document.querySelector(".popup-triceps");
let isShow_Triceps = true;

function showTriceps() {
  if (isShow_Triceps) {
      popup_Triceps.style.display = "block";
      isShow_Triceps = false;
  } else {
      popup_Triceps.style.display = "none";
      isShow_Triceps = true;
  }
}

let popup_Forearm = document.querySelector(".popup-forearm");
let isShow_Forearm = true;
function showForearm(){
  if(isShow_Forearm){
  popup_Forearm.style.display = "block";
  isShow_Forearm = false;
  }
  else{
      popup_Forearm.style.display = "none";
  isShow_Forearm = true;
  }
}
let popup_Upperchest = document.querySelector(".popup-upperchest");
let isShow_Upperchest = true;
function showUpperChest(){
  if(isShow_Upperchest){
  popup_Upperchest.style.display = "block";
  isShow_Upperchest = false;
  }
  else{
      popup_Upperchest.style.display = "none";
  isShow_Upperchest = true;
  }
}
let popup_Lowerchest = document.querySelector(".popup-lowerchest");
let isShow_Lowerchest = true;

function showlowerChest() {
  if (isShow_Lowerchest) {
      popup_Lowerchest.style.display = "block";
      isShow_Lowerchest = false;
  } else {
      popup_Lowerchest.style.display = "none";
      isShow_Lowerchest = true;
  }
}

let popup_Innerchest = document.querySelector(".popup-innerchest");
let isShow_Innerchest = true;
function showInnerChest(){
  if(isShow_Innerchest){
  popup_Innerchest.style.display = "block";
  isShow_Innerchest = false;
  }
  else{
      popup_Innerchest.style.display = "none";
  isShow_Innerchest = true;
  }
}
let popup_Middlechest = document.querySelector(".popup-middlechest");
let isShow_Middlechest = true;
function showMiddleChest(){
  if(isShow_Middlechest){
  popup_Middlechest.style.display = "block";
  isShow_Middlechest = false;
  }
  else{
      popup_Middlechest.style.display = "none";
  isShow_Middlechest = true;
  }
}
let popup_Trapezius = document.querySelector(".popup-trapezius");
let isShow_Trapezius = true;
function showTrapezius(){
  if(isShow_Trapezius){
  popup_Trapezius.style.display = "block";
  isShow_Trapezius = false;
  }
  else{
      popup_Trapezius.style.display = "none";
  isShow_Trapezius = true;
  }
}
let popup_Lats= document.querySelector(".popup-lats");
let isShow_Lats = true;
function showLats(){
  if(isShow_Lats){
  popup_Lats.style.display = "block";
  isShow_Lats = false;
  }
  else{
      popup_Lats.style.display = "none";
  isShow_Lats = true;
  }
}
let popup_Rhombodis = document.querySelector(".popup-rhombodis");
let isShow_Rhombodis = true;

function showRhomboids() {
  if (isShow_Rhombodis) {
      popup_Rhombodis.style.display = "block";
      isShow_Rhombodis = false;
  } else {
      popup_Rhombodis.style.display = "none";
      isShow_Rhombodis = true;
  }
}

let popup_Lowerback = document.querySelector(".popup-lowerback");
let isShow_Lowerback = true;
function showLowerBack(){
  if(isShow_Lowerback){
  popup_Lowerback.style.display = "block";
  isShow_Lowerback = false;
  }
  else{
      popup_Lowerback.style.display = "none";
  isShow_Lowerback = true;
  }
}
let popup_Upperabs = document.querySelector(".popup-upperabs");
let isShow_Upperabs = true;
function showUpperAbs(){
  if(isShow_Upperabs){
  popup_Upperabs.style.display = "block";
  isShow_Upperabs = false;
  }
  else{
      popup_Upperabs.style.display = "none";
  isShow_Upperabs = true;
  }
}
let popup_Lowerabs = document.querySelector(".popup-lowerabs");
let isShow_Lowerabs = true;
function showLowerAbs(){
  if(isShow_Lowerabs){
  popup_Lowerabs.style.display = "block";
  isShow_Lowerabs = false;
  }
  else{
      popup_Lowerabs.style.display = "none";
  isShow_Lowerabs = true;
  }
}
let popup_Quads= document.querySelector(".popup-quads");
let isShow_Quads = true;
function showQuads(){
  if(isShow_Quads){
  popup_Quads.style.display = "block";
  isShow_Quads = false;
  }
  else{
      popup_Quads.style.display = "none";
  isShow_Quads = true;
  }
}
let popup_Hamstrings = document.querySelector(".popup-hamstrings");
let isShow_Hamstrings = true;
function showHamstrings(){
  if(isShow_Hamstrings){
  popup_Hamstrings.style.display = "block";
  isShow_Hamstrings = false;
  }
  else{
      popup_Hamstrings.style.display = "none";
  isShow_Hamstrings = true;
  }
}
let popup_Glutes = document.querySelector(".popup-glutes");
let isShow_Glutes = true;
function showGlutes(){
  if(isShow_Glutes){
  popup_Glutes.style.display = "block";
  isShow_Glutes = false;
  }
  else{
      popup_Glutes.style.display = "none";
  isShow_Glutes = true;
  }
}
let popup_Claves = document.querySelector(".popup-claves");
let isShow_Claves = true;
function showClaves(){
  if(isShow_Claves){
  popup_Claves.style.display = "block";
  isShow_Claves = false;
  }
  else{
      popup_Claves.style.display = "none";
  isShow_Claves = true;
  }
}
let search_box = document.querySelector(".search-box");
let isSearch = true;
function showSearch(){
  if(isSearch){
      search_box.style.display = "block";
      isSearch = false;
  }
  else{
      search_box.style.display = "none";
      isSearch = true;
  }
}
let available=[
  'shoulders',
  'biceps',
  'triceps',
  'forearm',
  'upperchest',
  'lowerchest',
  'innerchest',
  'middlechest',
  'trapezius',
  'lats',
  'rhomboids',
  'lowerback',
  'upperabs',
  'lowerabs',
  'quads',
  'hamstrings',
  'glutes',
  'claves',
];

const resultsBox = document.querySelector(".result-box");
const inputBox= document.getElementById("input-box");

inputBox.onkeyup = function(){
  let result=[];
  let input = inputBox.value;
  
  if(input.length){
      result = available.filter((keyword)=>{
         return keyword.toLowerCase().includes(input.toLowerCase());

      });
      console.log(result);
  }
  display(result)
  if(!result.length){
      resultsBox.innerHTML='';
  }
}
function display(result){
  const content = result.map((list)=>{
      return "<li onclick=selectInput(this);show"+list+"Vid()>" + list + "</li>"

  });

  resultsBox.innerHTML = "<ul>" + content.join('') + "</ul>";
}
function selectInput(list){
  inputBox.value = list.innerHTML;
  resultsBox.innerHTML = '';
}
let bicep_exe = document.querySelector(".vid-bicep");
let isShow_bicep_vid = true;
function showbicepsVid(){ 
  if(isShow_bicep_vid){
      bicep_exe.style.display = "block";
  isShow_bicep_vid = false;
  }
  else{
      bicep_exe.style.display = "none";
      isShow_bicep_vid = true;
  }
}
let shoulder_exe = document.querySelector(".vid-shoulder");
let isShow_shoulder_vid = true;
function showshouldersVid(){ 
  if(isShow_shoulder_vid){
      shoulder_exe.style.display = "block";
  isShow_shoulder_vid = false;
  }
  else{
      shoulder_exe.style.display = "none";
      isShow_shoulder_vid = true;
  }
}
let tricep_exe = document.querySelector(".vid-tricep");
let isShow_tricep_vid = true;
function showtricepsVid(){ 
  if(isShow_tricep_vid){
      tricep_exe.style.display = "block";
  isShow_tricep_vid = false;
  }
  else{
      tricep_exe.style.display = "none";
      isShow_tricep_vid = true;
  }
}
let forearm_exe = document.querySelector(".vid-forearm");
let isShow_forearm_vid = true;
function showforearmVid(){ 
  if(isShow_forearm_vid){
      forearm_exe.style.display = "block";
  isShow_forearm_vid = false;
  }
  else{
      forearm_exe.style.display = "none";
      isShow_forearm_vid = true;
  }
}
let upperchest_exe = document.querySelector(".vid-upperchest");
let isShow_upperchest_vid = true;
function showupperchestVid(){ 
  if(isShow_upperchest_vid){
      upperchest_exe.style.display = "block";
  isShow_upperchest_vid = false;
  }
  else{
      upperchest_exe.style.display = "none";
      isShow_upperchest_vid = true;
  }
}
let lowerchest_exe = document.querySelector(".vid-lowerchest");
let isShow_lowerchest_vid = true;
function showlowerchestVid(){ 
  if(isShow_lowerchest_vid){
      lowerchest_exe.style.display = "block";
  isShow_lowerchest_vid = false;
  }
  else{
      lowerchest_exe.style.display = "none";
      isShow_lowerchest_vid = true;
  }
}
let innerchest_exe = document.querySelector(".vid-innerchest");
let isShow_innerchest_vid = true;
function showlowerchestVid(){ 
  if(isShow_innerchest_vid){
      innerchest_exe.style.display = "block";
  isShow_innerchest_vid = false;
  }
  else{
      innerchest_exe.style.display = "none";
      isShow_innerchest_vid = true;
  }
}
let trapezius_exe = document.querySelector(".vid-trapezius");
let isShow_trapezius_vid = true;
function showtrapeziusVid(){ 
  if(isShow_trapezius_vid){
      trapezius_exe.style.display = "block";
  isShow_trapezius_vid = false;
  }
  else{
      trapezius_exe.style.display = "none";
      isShow_trapezius_vid = true;
  }
}
let lats_exe = document.querySelector(".vid-lats");
let isShow_lats_vid = true;
function showlatsVid(){ 
  if(isShow_lats_vid){
      trapezius_exe.style.display = "block";
  isShow_lats_vid = false;
  }
  else{
      lats_exe.style.display = "none";
      isShow_lats_vid = true;
  }
}
let rhombodis_exe = document.querySelector(".vid-rhombodis");
let isShow_rhombodis_vid = true;
function showrlatsVid(){ 
  if(isShow_rhombodis_vid){
      rhombodis_exe.style.display = "block";
  isShow_rhombodis_vid = false;
  }
  else{
      rhombodis_exe.style.display = "none";
      isShow_rhombodis_vid = true;
  }
}
let lowerback_exe = document.querySelector(".vid-lowerback");
let isShow_lowerback_vid = true;
function showlatsVid(){ 
  if(isShow_lowerback_vid){
      lowerback_exe.style.display = "block";
  isShow_lowerback_vid = false;
  }
  else{
      lowerback_exe.style.display = "none";
      isShow_lowerback_vid = true;
  }
}
let upperabs_exe = document.querySelector(".vid-upperabs");
let isShow_upperabs_vid = true;
function showupperabsVid(){ 
  if(isShow_upperabs_vid){
      upperabs_exe.style.display = "block";
  isShow_upperabs_vid = false;
  }
  else{
      upperabs_exe.style.display = "none";
      isShow_upperabs_vid = true;
  }
}
let lowerabs_exe = document.querySelector(".vid-lowerabs");
let isShow_lowerabs_vid = true;
function showlowerabsVid(){ 
  if(isShow_lowerabs_vid){
      lowerabs_exe.style.display = "block";
  isShow_lowerabs_vid = false;
  }
  else{
      lowerabs_exe.style.display = "none";
      isShow_lowerabs_vid = true;
  }
}
let quads_exe = document.querySelector(".vid-quads");
let isShow_quads_vid = true;
function showquadsVid(){ 
  if(isShow_quads_vid){
     quads_exe.style.display = "block";
  isShow_quads_vid = false;
  }
  else{
      quads_exe.style.display = "none";
      isShow_quads_vid = true;
  }
}
let hamstrings_exe = document.querySelector(".vid-hamstrings");
let isShow_hamstrings_vid = true;
function showhamstringsVid(){ 
  if(isShow_hamstrings_vid){
      hamstrings_exe.style.display = "block";
  isShow_hamstrings_vid = false;
  }
  else{
      hamstrings_exe.style.display = "none";
      isShow_hamstrings_vid = true;
  }
}
let glutes_exe = document.querySelector(".vid-glutes");
let isShow_glutes_vid = true;
function showglutesVid(){ 
  if(isShow_glutes_vid){
      glutes_exe.style.display = "block";
  isShow_glutes_vid = false;
  }
  else{
      glutes_exe.style.display = "none";
      isShow_glutes_vid = true;
  }
}
let claves_exe = document.querySelector(".vid-claves");
let isShow_claves_vid = true;
function showclavesVid(){ 
  if(isShow_claves_vid){
      claves_exe.style.display = "block";
  isShow_claves_vid = false;
  }
  else{
      claves_exe.style.display = "none";
      isShow_claves_vid = true;
  }
}
let cal_popup = document.querySelector(".cal-container");
let isShow_cal_popup  = true;
function showcalh2(){ 
  if(isShow_cal_popup){
      cal_popup.style.display = "block";
      isShow_cal_popup = false;
  }
  else{
      cal_popup.style.display = "none";
      isShow_cal_popup = true;
  }
}
let cal_h2 = document.querySelector(".cont-h2");
let isShow_cal_h2  = true;
function showcalender(){ 
  if(isShow_cal_h2){
      cal_h2.style.display = "none";
      isShow_cal_h2 = false;
  }
  else{
      cal_h2.style.display = "block";
      isShow_cal_h2 = true;
  }
}

const calendar = document.querySelector(".calendar"),
date = document.querySelector(".date"),
daysContainer = document.querySelector(".days"),
prev = document.querySelector(".prev"),
next = document.querySelector(".next"),
todayBtn = document.querySelector(".today-btn"),
gotoBtn = document.querySelector(".goto-btn"),
dateInput = document.querySelector(".date-input"),
eventDay = document.querySelector(".event-day"),
eventDate = document.querySelector(".event-date"),
eventsContainer = document.querySelector(".events"),
addEventBtn = document.querySelector(".add-event"),
addEventWrapper = document.querySelector(".add-event-wrapper "),
addEventCloseBtn = document.querySelector(".close "),
addEventTitle = document.querySelector(".event-name "),
addEventFrom = document.querySelector(".event-time-from "),
addEventTo = document.querySelector(".event-time-to "),
addEventSubmit = document.querySelector(".add-event-btn ");

let today = new Date();
let activeDay;
let month = today.getMonth();
let year = today.getFullYear();

const months = [
"January",
"February",
"March",
"April",
"May",
"June",
"July",
"August",
"September",
"October",
"November",
"December",
];

const eventsArr = [];
getEvents();
console.log(eventsArr);


function initCalendar() {
const firstDay = new Date(year, month, 1);
const lastDay = new Date(year, month + 1, 0);
const prevLastDay = new Date(year, month, 0);
const prevDays = prevLastDay.getDate();
const lastDate = lastDay.getDate();
const day = firstDay.getDay();
const nextDays = 7 - lastDay.getDay() - 1;

date.innerHTML = months[month] + " " + year;

let days = "";

for (let x = day; x > 0; x--) {
  days += `<div class="day prev-date">${prevDays - x + 1}</div>`;
}

for (let i = 1; i <= lastDate; i++) {
  let event = false;
  eventsArr.forEach((eventObj) => {
    if (
      eventObj.day === i &&
      eventObj.month === month + 1 &&
      eventObj.year === year
    ) {
      event = true;
    }
  });
  if (
    i === new Date().getDate() &&
    year === new Date().getFullYear() &&
    month === new Date().getMonth()
  ) {
    activeDay = i;
    getActiveDay(i);
    updateEvents(i);
    if (event) {
      days += `<div class="day today active event">${i}</div>`;
    } else {
      days += `<div class="day today active">${i}</div>`;
    }
  } else {
    if (event) {
      days += `<div class="day event">${i}</div>`;
    } else {
      days += `<div class="day ">${i}</div>`;
    }
  }
}

for (let j = 1; j <= nextDays; j++) {
  days += `<div class="day next-date">${j}</div>`;
}
daysContainer.innerHTML = days;
addListner();
}

function prevMonth() {
month--;
if (month < 0) {
  month = 11;
  year--;
}
initCalendar();
}

function nextMonth() {
month++;
if (month > 11) {
  month = 0;
  year++;
}
initCalendar();
}

prev.addEventListener("click", prevMonth);
next.addEventListener("click", nextMonth);

initCalendar();

function addListner() {
const days = document.querySelectorAll(".day");
days.forEach((day) => {
  day.addEventListener("click", (e) => {
    getActiveDay(e.target.innerHTML);
    updateEvents(Number(e.target.innerHTML));
    activeDay = Number(e.target.innerHTML);

    days.forEach((day) => {
      day.classList.remove("active");
    });

    if (e.target.classList.contains("prev-date")) {
      prevMonth();
      setTimeout(() => {
        const days = document.querySelectorAll(".day");
        days.forEach((day) => {
          if (
            !day.classList.contains("prev-date") &&
            day.innerHTML === e.target.innerHTML
          ) {
            day.classList.add("active");
          }
        });
      }, 100);
    } else if (e.target.classList.contains("next-date")) {
      nextMonth();
      setTimeout(() => {
        const days = document.querySelectorAll(".day");
        days.forEach((day) => {
          if (
            !day.classList.contains("next-date") &&
            day.innerHTML === e.target.innerHTML
          ) {
            day.classList.add("active");
          }
        });
      }, 100);
    } else {
      e.target.classList.add("active");
    }
  });
});
}

todayBtn.addEventListener("click", () => {
today = new Date();
month = today.getMonth();
year = today.getFullYear();
initCalendar();
});

dateInput.addEventListener("input", (e) => {
dateInput.value = dateInput.value.replace(/[^0-9/]/g, "");
if (dateInput.value.length === 2) {
  dateInput.value += "/";
}
if (dateInput.value.length > 7) {
  dateInput.value = dateInput.value.slice(0, 7);
}
if (e.inputType === "deleteContentBackward") {
  if (dateInput.value.length === 3) {
    dateInput.value = dateInput.value.slice(0, 2);
  }
}
});

gotoBtn.addEventListener("click", gotoDate);

function gotoDate() {
console.log("here");
const dateArr = dateInput.value.split("/");
if (dateArr.length === 2) {
  if (dateArr[0] > 0 && dateArr[0] < 13 && dateArr[1].length === 4) {
    month = dateArr[0] - 1;
    year = dateArr[1];
    initCalendar();
    return;
  }
}
alert("Invalid Date");
}

function getActiveDay(date) {
const day = new Date(year, month, date);
const dayName = day.toString().split(" ")[0];
eventDay.innerHTML = dayName;
eventDate.innerHTML = date + " " + months[month] + " " + year;
}

function updateEvents(date) {
let events = "";
eventsArr.forEach((event) => {
  if (
    date === event.day &&
    month + 1 === event.month &&
    year === event.year
  ) {
    event.events.forEach((event) => {
      events += `<div class="event">
          <div class="title">
            <i class="fas fa-circle"></i>
            <h3 class="event-title">${event.title}</h3>
          </div>
          <div class="event-time">
            <span class="event-time">${event.time}</span>
          </div>
      </div>`;
    });
  }
});
if (events === "") {
  events = `<div class="no-event">
          <h3>No Events</h3>
      </div>`;
}
eventsContainer.innerHTML = events;
saveEvents();
}

addEventBtn.addEventListener("click", () => {
addEventWrapper.classList.toggle("active");
});

addEventCloseBtn.addEventListener("click", () => {
addEventWrapper.classList.remove("active");
});

document.addEventListener("click", (e) => {
if (e.target !== addEventBtn && !addEventWrapper.contains(e.target)) {
  addEventWrapper.classList.remove("active");
}
});

addEventTitle.addEventListener("input", (e) => {
addEventTitle.value = addEventTitle.value.slice(0, 60);
});


addEventFrom.addEventListener("input", (e) => {
addEventFrom.value = addEventFrom.value.replace(/[^0-9:]/g, "");
if (addEventFrom.value.length === 2) {
  addEventFrom.value += ":";
}
if (addEventFrom.value.length > 5) {
  addEventFrom.value = addEventFrom.value.slice(0, 5);
}
});

addEventTo.addEventListener("input", (e) => {
addEventTo.value = addEventTo.value.replace(/[^0-9:]/g, "");
if (addEventTo.value.length === 2) {
  addEventTo.value += ":";
}
if (addEventTo.value.length > 5) {
  addEventTo.value = addEventTo.value.slice(0, 5);
}
});

addEventSubmit.addEventListener("click", () => {
const eventTitle = addEventTitle.value;
const eventTimeFrom = addEventFrom.value;
const eventTimeTo = addEventTo.value;
if (eventTitle === "" || eventTimeFrom === "" || eventTimeTo === "") {
  alert("Please fill all the fields");
  return;
}

const timeFromArr = eventTimeFrom.split(":");
const timeToArr = eventTimeTo.split(":");
if (
  timeFromArr.length !== 2 ||
  timeToArr.length !== 2 ||
  timeFromArr[0] > 23 ||
  timeFromArr[1] > 59 ||
  timeToArr[0] > 23 ||
  timeToArr[1] > 59
) {
  alert("Invalid Time Format");
  return;
}

const timeFrom = convertTime(eventTimeFrom);
const timeTo = convertTime(eventTimeTo);

let eventExist = false;
eventsArr.forEach((event) => {
  if (
    event.day === activeDay &&
    event.month === month + 1 &&
    event.year === year
  ) {
    event.events.forEach((event) => {
      if (event.title === eventTitle) {
        eventExist = true;
      }
    });
  }
});
if (eventExist) {
  alert("Event already added");
  return;
}
const newEvent = {
  title: eventTitle,
  time: timeFrom + " - " + timeTo,
};
console.log(newEvent);
console.log(activeDay);
let eventAdded = false;
if (eventsArr.length > 0) {
  eventsArr.forEach((item) => {
    if (
      item.day === activeDay &&
      item.month === month + 1 &&
      item.year === year
    ) {
      item.events.push(newEvent);
      eventAdded = true;
    }
  });
}

if (!eventAdded) {
  eventsArr.push({
    day: activeDay,
    month: month + 1,
    year: year,
    events: [newEvent],
  });
}

console.log(eventsArr);
addEventWrapper.classList.remove("active");
addEventTitle.value = "";
addEventFrom.value = "";
addEventTo.value = "";
updateEvents(activeDay);
const activeDayEl = document.querySelector(".day.active");
if (!activeDayEl.classList.contains("event")) {
  activeDayEl.classList.add("event");
}
});

eventsContainer.addEventListener("click", (e) => {
if (e.target.classList.contains("event")) {
  if (confirm("Are you sure you want to delete this event?")) {
    const eventTitle = e.target.children[0].children[1].innerHTML;
    eventsArr.forEach((event) => {
      if (
        event.day === activeDay &&
        event.month === month + 1 &&
        event.year === year
      ) {
        event.events.forEach((item, index) => {
          if (item.title === eventTitle) {
            event.events.splice(index, 1);
          }
        });
        if (event.events.length === 0) {
          eventsArr.splice(eventsArr.indexOf(event), 1);
          const activeDayEl = document.querySelector(".day.active");
          if (activeDayEl.classList.contains("event")) {
            activeDayEl.classList.remove("event");
          }
        }
      }
    });
    updateEvents(activeDay);
  }
}
});

function saveEvents() {
localStorage.setItem("events", JSON.stringify(eventsArr));
}

function getEvents() {
if (localStorage.getItem("events") === null) {
  return;
}
eventsArr.push(...JSON.parse(localStorage.getItem("events")));
}

function convertTime(time) {
let timeArr = time.split(":");
let timeHour = timeArr[0];
let timeMin = timeArr[1];
let timeFormat = timeHour >= 12 ? "PM" : "AM";
timeHour = timeHour % 12 || 12;
time = timeHour + ":" + timeMin + " " + timeFormat;
return time;
}
let diet_popup = document.querySelector(".diet-popup");
let dietcalsearch = document.querySelector(".diet-cal-search-popup");
let body = document.querySelector(".body");
let isdietcalsearch = true;
function showdietcalsearch(){
  if(isdietcalsearch){
    dietcalsearch.style.display = "flex";

    isdietcalsearch = false;
  }
  else{
    dietcalsearch.style.display = "none";
    isdietcalsearch = true;
  }
}

let isdiet = true;
function showdiet(){
  if(isdiet){
    diet_popup.style.display = "block";
    dietcalsearch.style.display = "none";
      isdiet = false;
  }
  else{
    diet_popup.style.display = "none";
      isdiet = true;
  }
}
let diet_menu_popup_btn = document.querySelector(".menu-btn-diet");
let isdietmenubtn = true;
function showdietmenubtn(){
  if(isdietmenu){
    diet_menu_popup_btn.style.display = "block";
      isdietmenubtn = false;
  }
  else{
    diet_menu_popup_btn.style.display = "none";
      isdietmenubtn = true;
  }
}
let header = document.querySelector(".showheader");
let diet_menu_popup = document.querySelector(".menu-diet");
let isdietmenu = true;
function showdietmenu(){
  if(isdietmenu){
    diet_menu_popup.style.display = "none";
    header.style.display = "none";
      isdietmenu = false;
  }
  else{
    diet_menu_popup.style.display = "block";
      isdietmenu = true;
  }
}
let carbs_menu_popup = document.querySelector(".carbs-menu-popup");
let iscarbsmenu = true;
function showcarbsmenu(){
  if(iscarbsmenu){
    carbs_menu_popup.style.display = "block";
    fat_menu_popup.style.display = "none";
    protein_menu_popup.style.display = "none";
      iscarbsmenu = false;
  }
  else{
    carbs_menu_popup.style.display = "none";
      iscarbsmenu = true;
  }
}
let protein_menu_popup = document.querySelector(".protein-menu-popup");
let isproteinmenu = true;
function showproteinmenu(){
  if(isproteinmenu){
    protein_menu_popup.style.display = "block";
    fat_menu_popup.style.display = "none";
    carbs_menu_popup.style.display = "none";
      isproteinmenu = false;
  }
  else{
    protein_menu_popup.style.display = "none";
      isproteinmenu = true;
  }
}
let fat_menu_popup = document.querySelector(".fat-menu-popup");
let isfatmenu = true;
function showfatmenu(){
  if(isfatmenu){
    fat_menu_popup.style.display = "inline-block";
    protein_menu_popup.style.display = "none";
    carbs_menu_popup.style.display = "none";
      isfatmenu = false;
  }
  else{
    fat_menu_popup.style.display = "none";
      isfatmenu = true;
  }
}

let isheader = true;
function showdietheader(){
  if(isheader){
    header.style.display = "block";
      isheader = false;
  }
  else{
    header.style.display = "none";
      isheader = true;
  }
}
document.addEventListener("DOMContentLoaded", function() {
  const searchcal = document.querySelector(".input-group input");
  const searchcalbtn = document.querySelector(".input-group button");

  function checkcalory(query) {
    $.ajax({
      method: 'GET',
      url: 'https://api.api-ninjas.com/v1/nutrition?query=' + query,
      headers: { 'X-Api-Key': 'osY1wZhNnj53Y4kv/esA7w==3GbbeuCV9OjqhQOL'},
      contentType: 'application/json',
      success: function(result) {
        console.log(result)
          document.querySelector(".Carbohydrates").innerHTML=result[0].carbohydrates_total_g;
          document.querySelector(".cholesterol_mg").innerHTML=result[0].cholesterol_mg;
          document.querySelector(".fat_saturated_g").innerHTML=result[0].fat_saturated_g;
          document.querySelector(".fat_total_g").innerHTML=result[0].fat_total_g;
          document.querySelector(".fiber_g").innerHTML=result[0].fiber_g;
          document.querySelector(".potassium_mg").innerHTML=result[0].potassium_mg;
          document.querySelector(".protein_g").innerHTML=result[0].protein_g;
          document.querySelector(".sodium_mg").innerHTML=result[0].sodium_mg;
          document.querySelector(".sugar_g").innerHTML=result[0].sugar_g;
          document.querySelector(".calst2").innerHTML=result[0].calories;
          document.querySelector(".calst1").innerHTML=result[0].name;
          document.querySelector(".calst3").innerHTML=result[0].calories;
          document.querySelector(".jogmin").innerHTML=Math.round((result[0].calories/229)*60);
          document.querySelector(".yogamin").innerHTML=Math.round((result[0].calories/223)*60);
          document.querySelector(".gymmin").innerHTML=Math.round((result[0].calories/483)*60);
          document.querySelector(".runmin").innerHTML=Math.round((result[0].calories/294)*60);
      },
      error: function ajaxError(jqXHR) {
          console.error('Error: ', jqXHR.responseText);
      },
  });
  }

  searchcalbtn.addEventListener("click", function() {
      event.preventDefault()
      checkcalory(searchcal.value);
  });
});


